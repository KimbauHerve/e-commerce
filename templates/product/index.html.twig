{% extends 'base.html.twig' %}

{% block titre %}Nos produits | KINSHOP
{% endblock %}

{% block corps %}
	<h1>Nos produits</h1><hr>
	<div class="row">
		{% for product in products %}
			<div class="col-md-3">
				<div class="div-item text-center">

					<a href="{{ path('product',{'slug':product.slug})}}"><img src="uploads/{{product.illustration}}" class="img-fluid"></a>
					<h4>{{product.name}}</h4>
					<h6>{{product.subtitle}}</h6>
					<span>{{(product.price/100)|number_format(2,',','.')}}
						€
					</span>
				</div>
			</div>
		{% endfor %}
	</div>
{% endblock %}
